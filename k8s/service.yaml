apiVersion: v1
kind: Service
metadata:
  name: spring-boot-hello
  namespace: cicd-demo
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: spring-boot-hello
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30080
